# ============================================================
# Brewery Distribution App - Environment Configuration
# ============================================================
# Copy this file to .env and fill in your values.

# ── Database ──────────────────────────────────────────────────────
# Path to the SQLite database file (optional, defaults to ./data/brewery.db)
# DB_PATH=./data/brewery.db

# ── Legacy Google Sheets (only needed for migrate-to-sqlite.js) ──
# SPREADSHEET_ID=your_spreadsheet_id_here
# GOOGLE_KEY_FILE=credentials.json
# GOOGLE_SERVICE_ACCOUNT_KEY=...

# Server port (optional, defaults to 3000)
PORT=3000

# ── Webhooks ──────────────────────────────────────────────────────
# Secret token that authorises incoming Zapier webhook calls.
# Set this to a long random string (e.g. openssl rand -hex 32).
# If left unset, the /webhooks/zapier/order endpoint returns 503.
#
# In Zapier:  Action → Webhooks by Zapier → POST
#             Add header:  Authorization: Bearer <this value>
WEBHOOK_SECRET=your_webhook_secret_here

# ── Google OAuth (Login / Authentication) ─────────────────────────
# Create OAuth 2.0 credentials in Google Cloud Console:
#   1. Go to https://console.cloud.google.com → APIs & Services → Credentials
#   2. Create an OAuth 2.0 Client ID (Web application type)
#   3. Add your domain to "Authorised JavaScript origins"
#   4. Add <your-domain>/auth/google/callback to "Authorised redirect URIs"
#      (for local dev: http://localhost:3000/auth/google/callback)
#
GOOGLE_CLIENT_ID=your_google_oauth_client_id_here
GOOGLE_CLIENT_SECRET=your_google_oauth_client_secret_here

# The OAuth callback URL registered in Google Cloud Console.
# Defaults to /auth/google/callback (works for local dev without setting this).
# For production set the full URL, e.g. https://app.yourdomain.com/auth/google/callback
# GOOGLE_CALLBACK_URL=https://app.yourdomain.com/auth/google/callback

# Restrict sign-in to one or more Google Workspace domains (recommended).
# Only users whose Google account belongs to a listed domain will be allowed in.
# Separate multiple domains with commas (e.g. yourcompany.com,sales.yourcompany.com).
# Leave blank to allow any Google account (not recommended for internal tools).
GOOGLE_ALLOWED_DOMAIN=yourcompany.com

# Secret used to sign the session cookie.
# Set this to a long random string (e.g. openssl rand -hex 32).
SESSION_SECRET=your_session_secret_here

# ── Notification Webhooks ─────────────────────────────────────────
# URL to send outgoing notification webhooks to (e.g. Zapier catch hook).
# Notifications are POSTed as JSON with an Authorization: Bearer <WEBHOOK_SECRET> header.
# Leave blank to disable outgoing webhooks.
# This can also be configured in the Settings page within the app.
# NOTIFICATION_WEBHOOK_URL=https://hooks.zapier.com/hooks/catch/12345/abcdef/

# ── Email ────────────────────────────────────────────────────────
# Email sending uses the Google OAuth credentials above (no extra
# config needed). When a user logs in they grant Gmail send
# permission, and emails are sent directly from their own account.
